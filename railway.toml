# Railway Configuration for Seedstr Agent (Backend)
# Using Dockerfile for deployment

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
context = "."

[deploy]
startCommand = "node dist/agent/cli.js start"
restartPolicyType = "always"
restartPolicyMaxRetries = 10
